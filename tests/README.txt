Run tests in this folder's subdirectories by running the executable with the relevant input
script, e.g. "$ ../../build/beadrodpmer/beadrodpmer-bin -in real.in" and then diff the output
from the stable output files (which should NOT be overwritten), e.g.
"$ diff molnew.trj_1000.vtp stable.trj_1000.vtp".
